--[ DUPE UI v4.0 - VISUAL ONLY | NO REAL DUPE | ITEMS FROM BOTH BACKPACK & CHARACTER ]

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Backpack = LocalPlayer:WaitForChild("Backpack")
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

local dragging = false
local dragStart = nil
local startPos = nil

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DupeGUI"
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 380, 0, 540)
MainFrame.Position = UDim2.new(0.5, -190, 0.5, -270)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 22)
MainFrame.BackgroundTransparency = 0
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 14)
Corner.Parent = MainFrame

local Glow = Instance.new("Frame")
Glow.Size = UDim2.new(1, 6, 1, 6)
Glow.Position = UDim2.new(0, -3, 0, -3)
Glow.BackgroundColor3 = Color3.fromRGB(255, 30, 110)
Glow.BackgroundTransparency = 0.85
Glow.BorderSizePixel = 0
Glow.Parent = MainFrame
local GlowCorner = Instance.new("UICorner")
GlowCorner.CornerRadius = UDim.new(0, 16)
GlowCorner.Parent = Glow

local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 46)
TitleBar.BackgroundColor3 = Color3.fromRGB(255, 30, 110)
TitleBar.BackgroundTransparency = 0
TitleBar.Parent = MainFrame
local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 14)
TitleCorner.Parent = TitleBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -60, 1, 0)
TitleLabel.Position = UDim2.new(0, 18, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "DUPE SYSTEM v4"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextSize = 18
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TitleBar

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 34, 0, 34)
CloseBtn.Position = UDim2.new(1, -44, 0, 6)
CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.BackgroundTransparency = 0.85
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 18
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Parent = TitleBar
local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(1, 0)
CloseCorner.Parent = CloseBtn

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

TitleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

local ItemsContainer = Instance.new("ScrollingFrame")
ItemsContainer.Size = UDim2.new(1, -24, 0, 330)
ItemsContainer.Position = UDim2.new(0, 12, 0, 62)
ItemsContainer.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
ItemsContainer.BackgroundTransparency = 0
ItemsContainer.BorderSizePixel = 0
ItemsContainer.CanvasSize = UDim2.new(0, 0, 0, 0)
ItemsContainer.ScrollBarThickness = 6
ItemsContainer.ScrollBarImageColor3 = Color3.fromRGB(255, 30, 110)
ItemsContainer.Parent = MainFrame
local ContainerCorner = Instance.new("UICorner")
ContainerCorner.CornerRadius = UDim.new(0, 10)
ContainerCorner.Parent = ItemsContainer

local ItemLayout = Instance.new("UIListLayout")
ItemLayout.Parent = ItemsContainer
ItemLayout.Padding = UDim.new(0, 8)
ItemLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local DupeBtn = Instance.new("TextButton")
DupeBtn.Size = UDim2.new(0.85, 0, 0, 54)
DupeBtn.Position = UDim2.new(0.075, 0, 1, -74)
DupeBtn.BackgroundColor3 = Color3.fromRGB(255, 30, 110)
DupeBtn.BackgroundTransparency = 0
DupeBtn.Text = "DUPLICATE SELECTED"
DupeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
DupeBtn.TextSize = 16
DupeBtn.Font = Enum.Font.GothamBold
DupeBtn.Parent = MainFrame
local DupeCorner = Instance.new("UICorner")
DupeCorner.CornerRadius = UDim.new(0, 10)
DupeCorner.Parent = DupeBtn

local StatusText = Instance.new("TextLabel")
StatusText.Size = UDim2.new(1, -24, 0, 30)
StatusText.Position = UDim2.new(0, 12, 1, -38)
StatusText.BackgroundTransparency = 1
StatusText.Text = "Select an item"
StatusText.TextColor3 = Color3.fromRGB(170, 170, 190)
StatusText.TextSize = 12
StatusText.Font = Enum.Font.Gotham
StatusText.Parent = MainFrame

local selectedItemName = nil
local selectedButton = nil
local itemButtons = {}

local function getAllItems()
    local items = {}
    for _, item in pairs(Backpack:GetChildren()) do
        if item:IsA("Tool") then
            table.insert(items, {name = item.Name, source = "Backpack"})
        end
    end
    if Character then
        for _, item in pairs(Character:GetChildren()) do
            if item:IsA("Tool") then
                table.insert(items, {name = item.Name, source = "Hand"})
            end
        end
    end
    return items
end

local function updateInventoryDisplay()
    for _, btn in pairs(itemButtons) do
        btn:Destroy()
    end
    itemButtons = {}
    
    local items = getAllItems()
    local canvasY = 0
    
    for _, info in pairs(items) do
        local itemBtn = Instance.new("TextButton")
        itemBtn.Size = UDim2.new(0.94, 0, 0, 48)
        itemBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
        itemBtn.BackgroundTransparency = 0
        local sourceIcon = (info.source == "Hand") and "✋ " or "🎒 "
        itemBtn.Text = sourceIcon .. info.name
        itemBtn.TextColor3 = Color3.fromRGB(220, 220, 240)
        itemBtn.TextSize = 14
        itemBtn.Font = Enum.Font.GothamSemibold
        itemBtn.TextXAlignment = Enum.TextXAlignment.Left
        itemBtn.Parent = ItemsContainer
        local BtnCorner = Instance.new("UICorner")
        BtnCorner.CornerRadius = UDim.new(0, 8)
        BtnCorner.Parent = itemBtn
        
        local iconFrame = Instance.new("Frame")
        iconFrame.Size = UDim2.new(0, 34, 1, 0)
        iconFrame.BackgroundColor3 = Color3.fromRGB(255, 30, 110)
        iconFrame.BackgroundTransparency = 0.7
        iconFrame.Parent = itemBtn
        local IconCorner = Instance.new("UICorner")
        IconCorner.CornerRadius = UDim.new(0, 8)
        IconCorner.Parent = iconFrame
        
        itemBtn.MouseEnter:Connect(function()
            if selectedButton ~= itemBtn then
                itemBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 65)
            end
        end)
        
        itemBtn.MouseLeave:Connect(function()
            if selectedButton ~= itemBtn then
                itemBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
            end
        end)
        
        itemBtn.MouseButton1Click:Connect(function()
            if selectedButton then
                selectedButton.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
            end
            selectedButton = itemBtn
            selectedItemName = info.name
            itemBtn.BackgroundColor3 = Color3.fromRGB(130, 40, 180)
            StatusText.Text = "Selected: " .. selectedItemName .. " [" .. info.source .. "]"
            StatusText.TextColor3 = Color3.fromRGB(0, 255, 150)
            
            TweenService:Create(itemBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(140, 50, 190)}):Play()
        end)
        
        itemButtons[info.name .. info.source] = itemBtn
        canvasY = canvasY + 56
    end
    
    ItemsContainer.CanvasSize = UDim2.new(0, 0, 0, math.max(canvasY, 300))
    
    if #items == 0 then
        local emptyLabel = Instance.new("TextLabel")
        emptyLabel.Size = UDim2.new(1, 0, 0, 60)
        emptyLabel.BackgroundTransparency = 1
        emptyLabel.Text = "🕳️ No items (backpack or hands)"
        emptyLabel.TextColor3 = Color3.fromRGB(120, 120, 140)
        emptyLabel.TextSize = 14
        emptyLabel.Font = Enum.Font.Gotham
        emptyLabel.Parent = ItemsContainer
        ItemsContainer.CanvasSize = UDim2.new(0, 0, 0, 100)
    end
end

DupeBtn.MouseEnter:Connect(function()
    TweenService:Create(DupeBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(220, 20, 90)}):Play()
end)

DupeBtn.MouseLeave:Connect(function()
    TweenService:Create(DupeBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(255, 30, 110)}):Play()
end)

DupeBtn.MouseButton1Click:Connect(function()
    if not selectedItemName then
        StatusText.Text = "❌ No item selected!"
        StatusText.TextColor3 = Color3.fromRGB(255, 70, 70)
        wait(1.5)
        StatusText.Text = "Select an item"
        StatusText.TextColor3 = Color3.fromRGB(170, 170, 190)
        return
    end
    
    StatusText.Text = "🌀 Duplicating " .. selectedItemName .. " (visual)..."
    StatusText.TextColor3 = Color3.fromRGB(255, 200, 50)
    
    TweenService:Create(DupeBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(180, 0, 200)}):Play()
    
    wait(0.4)
    
    local fakeClone = Instance.new("Frame")
    fakeClone.Size = UDim2.new(0.85, 0, 0, 44)
    fakeClone.Position = UDim2.new(0.075, 0, 0.25, 0)
    fakeClone.BackgroundColor3 = Color3.fromRGB(0, 210, 110)
    fakeClone.BackgroundTransparency = 0.15
    fakeClone.BorderSizePixel = 0
    fakeClone.Parent = MainFrame
    local CloneCorner = Instance.new("UICorner")
    CloneCorner.CornerRadius = UDim.new(0, 8)
    CloneCorner.Parent = fakeClone
    
    local cloneText = Instance.new("TextLabel")
    cloneText.Size = UDim2.new(1, 0, 1, 0)
    cloneText.BackgroundTransparency = 1
    cloneText.Text = "✨ +1 " .. selectedItemName .. " [VISUAL ONLY]"
    cloneText.TextColor3 = Color3.fromRGB(0, 255, 150)
    cloneText.TextSize = 13
    cloneText.Font = Enum.Font.GothamBold
    cloneText.Parent = fakeClone
    
    for i = 1, 30 do
        wait(0.025)
        fakeClone.Position = UDim2.new(0.075, 0, 0.25, i * 2.5)
        fakeClone.BackgroundTransparency = 0.15 + (i / 30) * 0.85
        cloneText.TextTransparency = (i / 30)
    end
    
    fakeClone:Destroy()
    
    StatusText.Text = "⚠️ VISUAL duplicate done! NOT REAL item."
    StatusText.TextColor3 = Color3.fromRGB(255, 255, 100)
    TweenService:Create(DupeBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(255, 30, 110)}):Play()
    
    wait(2.5)
    StatusText.Text = "Select an item"
    StatusText.TextColor3 = Color3.fromRGB(170, 170, 190)
end)

LocalPlayer.CharacterAdded:Connect(function(newChar)
    Character = newChar
    updateInventoryDisplay()
end)

Backpack.ChildAdded:Connect(updateInventoryDisplay)
Backpack.ChildRemoved:Connect(updateInventoryDisplay)
if Character then
    Character.ChildAdded:Connect(updateInventoryDisplay)
    Character.ChildRemoved:Connect(updateInventoryDisplay)
end

updateInventoryDisplay()

local appearTween = TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back), {BackgroundTransparency = 0})
appearTween:Play()
