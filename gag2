-- FIXED: NEON DUPER v2.0 - NO MORE DUPLICATE REAL ITEMS
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Backpack = LocalPlayer:WaitForChild("Backpack")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

-- Kill old GUI
local oldGui = LocalPlayer.PlayerGui:FindFirstChild("NeonDupeGUI")
if oldGui then oldGui:Destroy() end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NeonDupeGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer.PlayerGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 450, 0, 580)
MainFrame.Position = UDim2.new(0.5, -225, 0.5, -290)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 20)
MainFrame.BackgroundTransparency = 0
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local NeonBorder = Instance.new("Frame")
NeonBorder.Size = UDim2.new(1, 6, 1, 6)
NeonBorder.Position = UDim2.new(0, -3, 0, -3)
NeonBorder.BackgroundColor3 = Color3.fromRGB(0, 255, 200)
NeonBorder.BackgroundTransparency = 0.6
NeonBorder.BorderSizePixel = 0
NeonBorder.Parent = MainFrame
local BorderCorner = Instance.new("UICorner")
BorderCorner.CornerRadius = UDim.new(0, 14)
BorderCorner.Parent = NeonBorder

local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 45)
TitleBar.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
TitleBar.BackgroundTransparency = 0.3
TitleBar.Parent = MainFrame
local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 12)
TitleCorner.Parent = TitleBar

local TitleText = Instance.new("TextLabel")
TitleText.Size = UDim2.new(1, -70, 1, 0)
TitleText.Position = UDim2.new(0, 15, 0, 0)
TitleText.BackgroundTransparency = 1
TitleText.Text = "🌀 NEON DUPER [FIXED]"
TitleText.TextColor3 = Color3.fromRGB(0, 255, 200)
TitleText.TextXAlignment = Enum.TextXAlignment.Left
TitleText.Font = Enum.Font.GothamBold
TitleText.TextSize = 16
TitleText.Parent = TitleBar

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 35, 0, 35)
CloseBtn.Position = UDim2.new(1, -45, 0, 5)
CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 40, 100)
CloseBtn.BackgroundTransparency = 0
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 18
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Parent = TitleBar
local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(1, 0)
CloseCorner.Parent = CloseBtn

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

local ResizeHandle = Instance.new("Frame")
ResizeHandle.Size = UDim2.new(0, 18, 0, 18)
ResizeHandle.Position = UDim2.new(1, -22, 1, -22)
ResizeHandle.BackgroundColor3 = Color3.fromRGB(0, 255, 200)
ResizeHandle.BackgroundTransparency = 0.5
ResizeHandle.BorderSizePixel = 0
ResizeHandle.Parent = MainFrame
local ResizeCorner = Instance.new("UICorner")
ResizeCorner.CornerRadius = UDim.new(0, 4)
ResizeCorner.Parent = ResizeHandle

local ItemsScroller = Instance.new("ScrollingFrame")
ItemsScroller.Size = UDim2.new(1, -24, 0, 380)
ItemsScroller.Position = UDim2.new(0, 12, 0, 58)
ItemsScroller.BackgroundColor3 = Color3.fromRGB(15, 15, 28)
ItemsScroller.BackgroundTransparency = 0
ItemsScroller.BorderSizePixel = 0
ItemsScroller.CanvasSize = UDim2.new(0, 0, 0, 0)
ItemsScroller.ScrollBarThickness = 6
ItemsScroller.ScrollBarImageColor3 = Color3.fromRGB(0, 255, 200)
ItemsScroller.Parent = MainFrame
local ScrollerCorner = Instance.new("UICorner")
ScrollerCorner.CornerRadius = UDim.new(0, 8)
ScrollerCorner.Parent = ItemsScroller

local ItemListLayout = Instance.new("UIListLayout")
ItemListLayout.Parent = ItemsScroller
ItemListLayout.Padding = UDim.new(0, 8)
ItemListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local BottomFrame = Instance.new("Frame")
BottomFrame.Size = UDim2.new(1, -24, 0, 80)
BottomFrame.Position = UDim2.new(0, 12, 1, -92)
BottomFrame.BackgroundColor3 = Color3.fromRGB(8, 8, 18)
BottomFrame.BackgroundTransparency = 0
BottomFrame.BorderSizePixel = 0
BottomFrame.Parent = MainFrame
local BottomCorner = Instance.new("UICorner")
BottomCorner.CornerRadius = UDim.new(0, 8)
BottomCorner.Parent = BottomFrame

local DupeButton = Instance.new("TextButton")
DupeButton.Size = UDim2.new(0.6, 0, 0, 44)
DupeButton.Position = UDim2.new(0.2, 0, 0.5, -22)
DupeButton.BackgroundColor3 = Color3.fromRGB(0, 200, 150)
DupeButton.BackgroundTransparency = 0
DupeButton.Text = "⚡ FAKE DUPE ⚡"
DupeButton.TextColor3 = Color3.fromRGB(0, 0, 0)
DupeButton.TextSize = 15
DupeButton.Font = Enum.Font.GothamBold
DupeButton.Parent = BottomFrame
local DupeCorner = Instance.new("UICorner")
DupeCorner.CornerRadius = UDim.new(0, 8)
DupeCorner.Parent = DupeButton

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, -10, 0, 22)
StatusLabel.Position = UDim2.new(0, 5, 1, -26)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "🔮 Select a real item"
StatusLabel.TextColor3 = Color3.fromRGB(160, 160, 200)
StatusLabel.TextSize = 11
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.Parent = BottomFrame

local CounterLabel = Instance.new("TextLabel")
CounterLabel.Size = UDim2.new(0.5, 0, 0, 20)
CounterLabel.Position = UDim2.new(0, 10, 0, 5)
CounterLabel.BackgroundTransparency = 1
CounterLabel.Text = "Fake: 0"
CounterLabel.TextColor3 = Color3.fromRGB(0, 255, 200)
CounterLabel.TextSize = 11
CounterLabel.Font = Enum.Font.Gotham
CounterLabel.TextXAlignment = Enum.TextXAlignment.Left
CounterLabel.Parent = BottomFrame

-- Data
local fakeItems = {}
local selectedRealItem = nil
local selectedButton = nil
local buttons = {}
local updateQueued = false

-- Get unique real items (no duplicates)
local function getUniqueRealItems()
    local itemMap = {}
    for _, tool in pairs(Backpack:GetChildren()) do
        if tool:IsA("Tool") then
            itemMap[tool.Name] = true
        end
    end
    local unique = {}
    for name in pairs(itemMap) do
        table.insert(unique, name)
    end
    table.sort(unique) -- optional, just for consistency
    return unique
end

-- Refresh list (with debounce)
local function refreshList()
    if updateQueued then return end
    updateQueued = true
    task.wait(0.05) -- allow multiple events to merge
    
    -- Clear existing buttons
    for _, btn in pairs(buttons) do
        btn:Destroy()
    end
    buttons = {}
    
    local realNames = getUniqueRealItems()
    local allItems = {}
    for _, name in ipairs(realNames) do
        table.insert(allItems, {name = name, type = "REAL"})
    end
    for _, name in ipairs(fakeItems) do
        table.insert(allItems, {name = name, type = "FAKE"})
    end
    
    local totalHeight = 0
    for _, info in ipairs(allItems) do
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0.96, 0, 0, 48)
        btn.BackgroundColor3 = (info.type == "FAKE") and Color3.fromRGB(45, 25, 70) or Color3.fromRGB(25, 25, 45)
        btn.BackgroundTransparency = 0
        btn.BorderSizePixel = 0
        btn.Parent = ItemsScroller
        
        local btnCorner = Instance.new("UICorner")
        btnCorner.CornerRadius = UDim.new(0, 8)
        btnCorner.Parent = btn
        
        local iconBar = Instance.new("Frame")
        iconBar.Size = UDim2.new(0, 38, 1, 0)
        iconBar.BackgroundColor3 = (info.type == "FAKE") and Color3.fromRGB(180, 50, 255) or Color3.fromRGB(0, 200, 150)
        iconBar.BackgroundTransparency = 0.2
        iconBar.Parent = btn
        local iconCorner = Instance.new("UICorner")
        iconCorner.CornerRadius = UDim.new(0, 8)
        iconCorner.Parent = iconBar
        
        local iconText = Instance.new("TextLabel")
        iconText.Size = UDim2.new(1, 0, 1, 0)
        iconText.BackgroundTransparency = 1
        iconText.Text = (info.type == "FAKE") and "🟣" or "📦"
        iconText.TextColor3 = Color3.fromRGB(255, 255, 255)
        iconText.TextSize = 22
        iconText.Font = Enum.Font.GothamBold
        iconText.Parent = iconBar
        
        local nameLabel = Instance.new("TextLabel")
        nameLabel.Size = UDim2.new(1, -50, 1, 0)
        nameLabel.Position = UDim2.new(0, 46, 0, 0)
        nameLabel.BackgroundTransparency = 1
        nameLabel.Text = info.name .. (info.type == "FAKE" and " [CLONE]" or "")
        nameLabel.TextColor3 = (info.type == "FAKE") and Color3.fromRGB(200, 160, 255) or Color3.fromRGB(255, 255, 255)
        nameLabel.TextSize = 14
        nameLabel.Font = Enum.Font.GothamSemibold
        nameLabel.TextXAlignment = Enum.TextXAlignment.Left
        nameLabel.Parent = btn
        
        local badge = Instance.new("TextLabel")
        badge.Size = UDim2.new(0, 70, 0, 18)
        badge.Position = UDim2.new(1, -80, 0.5, -9)
        badge.BackgroundColor3 = (info.type == "FAKE") and Color3.fromRGB(120, 30, 180) or Color3.fromRGB(0, 120, 100)
        badge.BackgroundTransparency = 0.4
        badge.Text = info.type == "FAKE" and "FAKE" or "REAL"
        badge.TextColor3 = Color3.fromRGB(255, 255, 255)
        badge.TextSize = 10
        badge.Font = Enum.Font.GothamBold
        badge.Parent = btn
        local badgeCorner = Instance.new("UICorner")
        badgeCorner.CornerRadius = UDim.new(1, 0)
        badgeCorner.Parent = badge
        
        btn.Metadata = info
        
        btn.MouseEnter:Connect(function()
            if selectedButton ~= btn then
                TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = (info.type == "FAKE") and Color3.fromRGB(65, 40, 95) or Color3.fromRGB(45, 45, 65)}):Play()
            end
        end)
        btn.MouseLeave:Connect(function()
            if selectedButton ~= btn then
                TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = (info.type == "FAKE") and Color3.fromRGB(45, 25, 70) or Color3.fromRGB(25, 25, 45)}):Play()
            end
        end)
        
        if info.type == "REAL" then
            btn.MouseButton1Click:Connect(function()
                if selectedButton then
                    TweenService:Create(selectedButton, TweenInfo.new(0.15), {BackgroundColor3 = (selectedButton.Metadata.type == "FAKE") and Color3.fromRGB(45, 25, 70) or Color3.fromRGB(25, 25, 45)}):Play()
                end
                selectedButton = btn
                selectedRealItem = info.name
                TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(80, 50, 140)}):Play()
                StatusLabel.Text = "✅ Selected: " .. info.name
                StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 150)
            end)
        else
            btn.MouseButton2Click:Connect(function()
                for i, fname in ipairs(fakeItems) do
                    if fname == info.name then
                        table.remove(fakeItems, i)
                        break
                    end
                end
                refreshList()
                CounterLabel.Text = "Fake: " .. #fakeItems
                StatusLabel.Text = "🗑️ Removed fake item"
                StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
                task.wait(1.2)
                StatusLabel.Text = selectedRealItem and ("Selected: " .. selectedRealItem) or "Select a real item"
                StatusLabel.TextColor3 = selectedRealItem and Color3.fromRGB(0, 255, 150) or Color3.fromRGB(160, 160, 200)
            end)
        end
        
        buttons[btn] = btn
        totalHeight = totalHeight + 56
    end
    
    ItemsScroller.CanvasSize = UDim2.new(0, 0, 0, math.max(totalHeight, 300))
    CounterLabel.Text = "Fake: " .. #fakeItems
    
    if #allItems == 0 then
        local emptyMsg = Instance.new("TextLabel")
        emptyMsg.Size = UDim2.new(1, 0, 0, 60)
        emptyMsg.BackgroundTransparency = 1
        emptyMsg.Text = "💀 No items in backpack.\nGet some tools first."
        emptyMsg.TextColor3 = Color3.fromRGB(120, 120, 160)
        emptyMsg.TextSize = 13
        emptyMsg.Font = Enum.Font.Gotham
        emptyMsg.TextWrapped = true
        emptyMsg.Parent = ItemsScroller
        ItemsScroller.CanvasSize = UDim2.new(0, 0, 0, 100)
    end
    
    updateQueued = false
end

-- Dupe action
DupeButton.MouseButton1Click:Connect(function()
    if not selectedRealItem then
        StatusLabel.Text = "❌ No real item selected!"
        StatusLabel.TextColor3 = Color3.fromRGB(255, 60, 60)
        TweenService:Create(DupeButton, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(200, 60, 60)}):Play()
        task.wait(0.2)
        TweenService:Create(DupeButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 200, 150)}):Play()
        task.wait(1)
        StatusLabel.Text = selectedRealItem and ("Selected: " .. selectedRealItem) or "Select a real item"
        StatusLabel.TextColor3 = selectedRealItem and Color3.fromRGB(0, 255, 150) or Color3.fromRGB(160, 160, 200)
        return
    end
    
    table.insert(fakeItems, selectedRealItem)
    refreshList()
    
    local flash = Instance.new("Frame")
    flash.Size = UDim2.new(1, 0, 1, 0)
    flash.BackgroundColor3 = Color3.fromRGB(0, 255, 200)
    flash.BackgroundTransparency = 0.6
    flash.Parent = MainFrame
    TweenService:Create(flash, TweenInfo.new(0.4), {BackgroundTransparency = 1}):Play()
    game:GetService("Debris"):AddItem(flash, 0.4)
    
    TweenService:Create(DupeButton, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(255, 100, 200)}):Play()
    task.wait(0.1)
    TweenService:Create(DupeButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 255, 100)}):Play()
    task.wait(0.2)
    TweenService:Create(DupeButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 200, 150)}):Play()
    
    StatusLabel.Text = "✨ FAKE copy added: " .. selectedRealItem
    StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 200)
    task.wait(1.8)
    StatusLabel.Text = "Selected: " .. selectedRealItem
    StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 150)
end)

-- Dragging
local dragging = false
local dragStartPos = nil
local dragStartMouse = nil

TitleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStartPos = MainFrame.Position
        dragStartMouse = input.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStartMouse
        MainFrame.Position = UDim2.new(dragStartPos.X.Scale, dragStartPos.X.Offset + delta.X, dragStartPos.Y.Scale, dragStartPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

-- Resizing
local resizing = false
local resizeStartSize = nil
local resizeStartMouse = nil

ResizeHandle.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        resizing = true
        resizeStartSize = MainFrame.Size
        resizeStartMouse = input.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if resizing and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - resizeStartMouse
        local newWidth = math.clamp(resizeStartSize.X.Offset + delta.X, 380, 800)
        local newHeight = math.clamp(resizeStartSize.Y.Offset + delta.Y, 480, 900)
        MainFrame.Size = UDim2.new(0, newWidth, 0, newHeight)
        ItemsScroller.Size = UDim2.new(1, -24, 0, newHeight - 170)
        BottomFrame.Position = UDim2.new(0, 12, 1, -92)
        BottomFrame.Size = UDim2.new(1, -24, 0, 80)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        resizing = false
    end
end)

-- Neon pulse
RunService.RenderStepped:Connect(function()
    local pulse = (math.sin(tick() * 3) + 1) / 2
    NeonBorder.BackgroundTransparency = 0.5 + pulse * 0.3
    ResizeHandle.BackgroundTransparency = 0.4 + pulse * 0.2
end)

-- Events (with debounced refresh)
local function onBackpackChange()
    refreshList()
end

Backpack.ChildAdded:Connect(onBackpackChange)
Backpack.ChildRemoved:Connect(onBackpackChange)

refreshList()

MainFrame.BackgroundTransparency = 1
TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Quad), {BackgroundTransparency = 0}):Play()

-- Glow effect
local glow = Instance.new("Frame")
glow.Size = UDim2.new(1, 8, 1, 8)
glow.Position = UDim2.new(0, -4, 0, -4)
glow.BackgroundColor3 = Color3.fromRGB(0, 255, 200)
glow.BackgroundTransparency = 0.8
glow.BorderSizePixel = 0
glow.Parent = DupeButton
local glowCorner = Instance.new("UICorner")
glowCorner.CornerRadius = UDim.new(0, 12)
glowCorner.Parent = glow

task.spawn(function()
    while glow and glow.Parent do
        for i = 1, 40 do
            if not glow or not glow.Parent then break end
            glow.BackgroundTransparency = 0.6 + math.sin(tick() * 5) * 0.2
            task.wait(0.05)
        end
        task.wait()
    end
end)
